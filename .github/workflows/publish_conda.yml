name: publish_conda

on:
  release:
    types: [published]
  workflow_dispatch:
  push:
    branches:
      - ci-conda  # for debugging


jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: publish-to-conda
      uses: ./.github/actions/publish-conda-action
      with:
        subdir: 'conda'
        anacondatoken: ${{ secrets.ANACONDA_TOKEN }}
        platforms: 'win osx linux'
